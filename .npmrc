engine-strict=true

# @se4bio scope lives on GitHub Packages. The _authToken is injected at
# build time via a BuildKit secret (see Dockerfile.production + the
# build-image.yml workflow) so we never commit a token.
@se4bio:registry=https://npm.pkg.github.com
